# Prevent permission issues in WSL2
unsafe-perm=true
legacy-peer-deps=true

# Skip optional dependencies that might cause issues
omit=optional

# Use a local cache to avoid permission issues
cache=/tmp/.npm-cache