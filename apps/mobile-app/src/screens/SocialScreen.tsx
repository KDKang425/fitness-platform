import { View, Text } from 'react-native';

export default function StatsScreen() {
  return (
    <View style={{ flex:1, justifyContent:'center', alignItems:'center', backgroundColor:'#111' }}>
      <Text style={{ color:'#fff' }}>Stats Screen (TODO)</Text>
    </View>
  );
}